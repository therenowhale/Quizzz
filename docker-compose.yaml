services:
  db:
    image: postgres:18
    container_name: quizzz-postgre
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U quizzz_app -d quizzz"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data: